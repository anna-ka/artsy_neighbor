March 9, 2026

Fix /vendor/profile/edit route in the router (VendorLive.ProfileForm → VendorLive.Profile.Form)
Add user_id injection in save_artist(:new, ...)
Then test the full create/edit profile flow
Take your time!

Claude created ordering or products and collections in vendor dashboard:

Clean. Here's what was done:

products.ex — new reorder_products/1 function that assigns positions 1, 2, 3… to a list of products in a single transaction. Works even when all positions are currently nil.

dashboard.ex — template:

Product loop changed to Enum.with_index(collection.products) to expose product_index
New narrow first column with ↑/↓ buttons; each button carries both phx-value-productid and phx-value-collectionid so the handler knows which list to reorder
dashboard.ex — two new handlers:

Build the new order with List.delete_at + List.insert_at (avoids nil swap issues entirely)
Call Products.reorder_products(new_order), then reload all collections


March 31:



Vendor dashboard: active/inactive status toggle, profile preview
Admin: all three status values for artists
Image filenames: switch to UUID in vendor_live/product_form.ex
Nav UI: the small-screen scrolling issue (deferred)
File storage: S3/Tigris before deployment


May 30, 2026
Artist profiles
firstname@example.com email and password DevPassword1!
 

 August 20
 Perhaps this sort of problem will be easier to find with thorough testing rather than UI: we need to test thoroughly many scenarios of how an order may change: an order maybe be in confirmed and unconfirmed state, a pick up may or may not have been scheduled or rescheduled, a token to pick up may or may not have been issued. In all this scenarios an attempt to modify the order can be made


 Sept. 14

 Now, the broader recap after your month off — here's where things stand:

Committed & pushed (29df158, confirmed in sync with origin/main — despite what my notes said last time about it being unpushed):

Full reviews_test.exs (51 tests) and orders_test.exs (68 tests)
Bug fixes found while writing those: cancel_order status guard, do_amend no longer resets status/token when a vendor edits order items mid-flow, vendor can add items too, default pickup address/instructions, real date/time pickers, plus 6 real bugs (2 in reviews.ex, 4 in orders.ex/conversations.ex)
Uncommitted right now — the artist hard-delete work above.

The app in broad strokes: a Phoenix LiveView local-art-marketplace. Artists (vendors) onboard, list products, buyers browse and order, pickup gets scheduled, orders complete, then both sides can leave reviews (vendor review, buyer review, per-product review) within a 14-day window. Admin has artist/product/category management. Test suite was at 494/494 (490 + these 4 new ones) last I ran it.

What's next on the list, roughly in priority order:

MISSING:
Flagging page (FlagLive.New at /flag/:subject_type/:subject_id) — reviews already write Flag records but there's no UI to create one yet
Admin /admin/reviews and /admin/flags pages
Email scaffolding — buyer gets a completion link by email when vendor schedules pickup (Swoosh is already a dependency)
Payment options — cash/Interac at door, needs a payment_method field on orders
Payment scaffolding — Logger stubs for charge/refund functions, Stripe to be wired in later
Styling pass, multi-item cart, delivery-completion flow — all explicitly deferred
Want me to commit this artist-delete work now, or hold off while you look things over?


MISSING:
On the UI/looks overhaul — no rush, but when you're ready there's a dataviz/artifact-design-adjacent design skill setup I can walk you through, and it's the kind of task worth not clearing mid-stream since visual iteration benefits from me seeing the history of what you liked/rejected. Good candidate for its own long-lived session rather than folding into general dev work.

ERROR:
Sept. 16
Conversation not updated with PubSub when buyer completes order. Vendor's view of the conversation remains the same, with ability to add/delete items, reschedule pick up etc.